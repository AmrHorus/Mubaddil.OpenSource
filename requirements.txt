# Mubaddil - Python Dependencies
# Core UI Framework
PySide6>=6.5.0

# Development & Testing
pytest>=7.0.0
mypy>=1.0.0
ruff>=0.1.0

# Packaging (optional, for production builds)
pyinstaller>=6.0.0

# Rust-Python Bridge (installed via maturin)
# Run: pip install maturin && maturin develop